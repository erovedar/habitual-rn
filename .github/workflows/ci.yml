name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: habitual
          POSTGRES_PASSWORD: habitual
          POSTGRES_DB: habitual
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U habitual"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://habitual:habitual@localhost:5432/habitual
      TEST_DATABASE_URL: postgres://habitual:habitual@localhost:5432/habitual
      JWT_SECRET: ci-test-secret
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run migrate:up
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run test
